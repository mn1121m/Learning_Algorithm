#include <stdio.h>

// Functions
int bin(int n, int k, int max);

// Main
int main(void)
{
    int n, k, max;

    //input
    scanf("%d %d %d", &n, &k, &max);

    //print
    return bin(n, k, max);
}
// Functions
int bin(int n, int k, int m)
{
    for(int i = 0; i <= n; i++) {
        
    }
}